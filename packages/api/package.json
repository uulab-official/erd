{
  "name": "@modelforge/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelforge/deploy-engine": "workspace:*",
    "@modelforge/schema-engine": "workspace:*",
    "@modelforge/sdk": "workspace:*",
    "appwrite": "^17.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
